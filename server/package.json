{
  "name": "server",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "takecare",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "nodemonConfig": {
    "delay": "500",
    "watch": [
      "./*"
    ],
    "ignore": [
      "*.json"
    ],
    "env": {
      "NODE_ENV": "development"
    },
    "ext": "js,json"
  },
  "dependencies": {
    "querystring": "^0.2.0"
  }
}
